function ProfileCard(props) {
  return(<div>
    <div>the title is {props.title}</div>
    <div>the handle is {props.handle}</div>
  </div>);
}

export default ProfileCard;
//here "props" is custom made 